---
const { label = 'Get Fernie Listing Alerts', href = '/contact/' } = Astro.props;
---
<div class="sticky-cta"><a class="btn" href={href}>{label}</a></div>

<style>
.sticky-cta{position:fixed; left:0; right:0; bottom:14px; display:flex; justify-content:center; z-index:50; pointer-events:none;}
.btn{pointer-events:auto; background:var(--brand); color:var(--brand-ink); font-weight:800; padding:.9rem 1.2rem; border-radius:999px; box-shadow:0 10px 24px rgba(0,0,0,.15);}
@media (min-width: 768px){ .sticky-cta{ display:none; } }
</style>
